## Packages
recharts | Visualization for security scores and metrics
framer-motion | Smooth animations for scanning states and transitions
date-fns | Date formatting for scan timestamps

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
  sans: ["var(--font-sans)"],
}
